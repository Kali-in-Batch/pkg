#!/bin/busybox bash

# Example library
# Here there would be functions and variables that are used by the scripts,
# but since this is an example, we'll just use a simple variable.

export string="Hello, world!"
